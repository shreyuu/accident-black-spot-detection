{
  "name": "@accident-black-spot-detection/firebase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Security rules, indexes, seed data and the automated rules test suite.",
  "scripts": {
    "test:rules": "node --test --test-concurrency=1 \"tests/*.test.mjs\"",
    "test:functions": "node --test --test-concurrency=1 \"tests/integration/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@accident-black-spot-detection/shared-types": "*",
    "firebase": "^12.16.0"
  }
}
