{
  "name": "@accident-black-spot-detection/functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Privileged server-side operations: account deletion, data export, the Places proxy and the orphaned-image sweep.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@accident-black-spot-detection/shared-types": "*",
    "@types/node": "^24.0.0",
    "eslint": "^9.39.5",
    "typescript": "~6.0.3",
    "@eslint/js": "^9.39.5",
    "typescript-eslint": "^8.47.0"
  }
}
